apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pagamento-pv
  labels:
    type: local
    name: mongodb-pagamento-pv
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/mongodb
